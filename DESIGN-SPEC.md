# Дизайн-спецификация — Таунхауси Шевченкове

Справочник для доверстки. Все значения — фактические из `src/styles/`, замерены в браузере
на контрольных ширинах **1440 / 768 / 390 px**.

> **Главное правило проекта:** макет готовится под импорт в Figma.
> Никаких `clamp()`, `vw`, дробных `line-height` и «ширины по тексту» у кнопок.
> Подробности — в разделе [10. Правила вёрстки](#10-правила-вёрстки).

---

## 1. Брейкпоинты и контейнер

Вёрстка **desktop-first**: базовые правила без медиазапроса, дальше `max-width`-ступени.

| Ступень | Медиазапрос | Контрольная ширина | `--container-pad` | Ширина контента |
|---|---|---|---|---|
| Desktop | — (по умолчанию, > 1280) | **1440** | `72px` | **1296px** |
| Laptop | `@media (max-width:1280px)` | — | `48px` | `vw − 96` |
| Tablet | `@media (max-width:1024px)` | **768** | `40px` | **688px** |
| Mobile | `@media (max-width:560px)` | **390** | `20px` | **350px** |

```css
.wrap { max-width: 1440px; margin: 0 auto; padding: 0 var(--container-pad); }
```

Дополнительные точки слома раскладки (исторические, менять не нужно):
`900px` — `.trail`/`.comms`/`.loc` в одну колонку · `820px` — `.foot-cta` в одну колонку ·
`768px` — размер бейджей генплана · `720px` — лид-модалка в колонку · `480px` — мелкие правки.

**Вертикальный ритм секций** — `--section-pt`: `96 / 80 / 56 / 48px` по ступеням выше.

> ⚠️ **Порядок импортов важен.** В `main.css` файлы подключаются по порядку;
> `responsive.css` — **последний**. Медиазапрос не повышает специфичность, поэтому
> переопределение из `hero.css` **не перебьёт** правило из `footer.css`.
> Мобильные оверрайды кладите в `responsive.css` либо в файл самого компонента.

---

## 2. Шрифт

**Onest** (Google Fonts), начертания в проекте: `400 / 500 / 600 / 700`.

```
font-family: 'Onest', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
```

Lora подключена в `<head>`, но **нигде не используется** — при чистке можно убрать из ссылки.

`line-height` **всегда указывается в px**, не множителем. Кегли — целые, преимущественно чётные.

---

## 3. Цвета

Токены объявлены в `src/styles/tokens.css`. Новые цвета добавляйте туда же, а не инлайном.

### Текст

| Токен | HEX | Где |
|---|---|---|
| `--c-ink` | `#17201D` | основной текст, заголовки, `body` |
| `--c-text` | `#5C665F` | описания, подзаголовки |
| `--c-mut` | `#AEB0A2` | «приглушённая» часть заголовков (`.mut`) |
| `--c-grey` | `#9A9C90` | плейсхолдеры, микро-подписи |
| `--c-eyebrow` | `#8A8F86` | надзаголовки (`.eb`, `.fcard-eb`) |

### Бренд / акценты

| Токен | HEX | Где |
|---|---|---|
| `--c-green` | `#294239` | тёмная кнопка, акцентная карточка, активный таб |
| `--c-green-dk` | `#1E322A` | `:hover` тёмной кнопки |
| `--c-accent` | `#BC6848` | акцентная кнопка, маркеры, пины |
| `--c-accent-dk` | `#A5573B` | `:hover` акцентной кнопки |

### Фоны

| Токен | HEX | Где |
|---|---|---|
| `--c-bg` | `#F5F2EA` | фон страницы, фон модалки |
| `--c-bg-alt` | `#F3F0E8` | фон секции «Комунікації», прелоадер |
| `--c-beige` | `#EEE8DC` | `.tcell.text`, `.adv-card`, `.rl-tab` |
| `--c-beige-2` | `#ECE7DC` | `.fcard-beige`, `.mleft`, `.room` |
| `--c-field` | `#FBF9F3` | фон полей ввода |
| `--c-surface` | `#E7E2D6` | плейсхолдер под изображением |
| `--c-surface-2` | `#E3DED1` | плейсхолдер `.rl-cell`, `.tcell.photo` |

### Линии и рамки

| Токен | HEX | Где |
|---|---|---|
| `--c-line` | `#E0DBCF` | разделители секций, `.crow`, `.hdr` |
| `--c-line-soft` | `#D8D4CA` | рамки иконочных кнопок |
| `--c-line-field` | `#DDD8CC` | рамка поля ввода |
| `--c-line-2` | `#E4E0D5` | разделители внутри модалки |
| `--c-beige-line` | `#E1DBCD` | рамка бежевых карточек |

### На тёмном фоне (внутри `--c-green`)

| Токен | HEX | Где |
|---|---|---|
| `--c-cream-1` | `#F5F1E7` | заголовок на зелёном |
| `--c-cream-2` | `#F3EFE4` | иконки на зелёном, `.loc-chip` |
| `--c-cream-3` | `#F8F3EA` | текст `.pill-accent` |
| `--c-cream-4` | `#F6F2E8` | иконка `.tcell.text.green` |
| `--c-green-tint` | `#C2CDC5` | описание на зелёном |
| `--c-green-tint-2` | `#93A79B` | подпись активного таба |

### Захардкоженные (единичные, токена нет)

`#7C817A` подпись статистики · `#3C453F` `.feats li` · `#E3EAE0`/`#D6E0D3` `.adv-card.tint` ·
`#ECE6D9`/`#DED8CA` `.gp-hint`, рамка карты · `#C7C2B4` рамка `.pill-ghost` ·
`#B7B3A8` точка-разделитель · `#C9C6BC` вторая половина градиента `.terms-h .tw` ·
`#E4E0D6` рамка `.mform` · `#C0392B` **ошибки форм**.

---

## 4. Типографика

Колонки: **Desktop** (>1024) / **Tablet** (561–1024, контроль 768) / **Mobile** (≤560, контроль 390).
Если ячейка Tablet/Mobile пустая — значение наследуется от предыдущей ступени.

### 4.1 Заголовки

| Роль | Класс | Desktop | Tablet | Mobile | Вес | letter-spacing |
|---|---|---|---|---|---|---|
| H1 (hero) | `.hero-h1` | **66 / 66** | 40 / 40 | — | 700 | `-.03em` |
| H1 подзаголовок | `.hero-h1 .rot` | **32 / 40** | 20 / 24 | — | 500 | `-.01em` |
| H2 секции | `.h-title` `.rl-title` `.gp-title` `.loc-title` | **52 / 54** | 28 / 30 | — | 700 | `-.025em` |
| H2 секции | `.tech-title` `.comms-h` | **52 / 56** | 28 / 30 | — | 700 | `-.025em` |
| H2 statement | `.terms-h` | **48 / 52** | 36 / 40 | 30 / 34 | 700 | `-.025em` |
| H3 крупный | `.crow-t` | **34 / 36** | 22 / 24 | — | 700 | `-.02em` |
| H3 на фото | `.hcard-t .a` | **34 / 34** | 28 / 28 | — | 700 | `-.02em` |
| H3 акцент-число | `.t-acc` | **52 / 48** | 34 / 32 | — | 700 | `-.04em` |
| H4 карточки | `.adv-name` | **24 / 26** | 20 / 22 | — | 700 | `-.02em` |
| H4 карточки (big) | `.adv-card.big .adv-name` | **30 / 34** | 22 / 24 | — | 700 | `-.02em` |

### 4.2 Заголовки в модалках

| Роль | Класс | Desktop | Tablet | Mobile | Вес | letter-spacing |
|---|---|---|---|---|---|---|
| Площадь дома | `.marea` | **56 / 56** | 34 / 34 | — | 700 | `-.035em` |
| Заголовок лид-формы | `.lform-h` | **42 / 44** | 28 / 30 | — | 700 | `-.03em` |
| Заголовок мини-формы | `.mform-h` | **20 / 26** | 18 / 22 | — | 600 | `-.01em` |

### 4.3 Основной текст

| Роль | Класс | Desktop | Tablet | Mobile | Вес |
|---|---|---|---|---|---|
| Body L | `.loc-text` | **18 / 28** | 16 / 24 | — | 400 |
| Body L | `.fcard-sub` | **18 / 26** | 16 / 24 | — | 400 |
| Body M | `.rl-text` `.lform-sub` | **16 / 24** | 16 / 24 | — | 400 |
| Body M | `.h-desc` | **16 / 24** | 14 / 22 | — | 400 |
| Body M | `.hero-blist li` | **16 / 24** | — | — | 500 |
| Body M | `.msec .mval` | **16 / 20** | — | — | 500 |
| Body S | `.t-d` `.crow-d` | **16 / 22** | 14 / 20 | — | 400 |
| Body S | `.adv-desc` `.feats li` | **14 / 22** | — | — | 400 |
| Body S | `.hcard-t .s` | **14 / 20** | — | — | 400 |

### 4.4 Интерфейсный текст

| Роль | Класс | Desktop | Tablet | Mobile | Вес |
|---|---|---|---|---|---|
| Кнопка | `.pill` | **15 / 15** | 15 / 15 | **16 / 16** | 600 |
| Навигация | `.nav a` | **15 / 15** | скрыта | скрыта | 500 |
| Навигация футера | `.foot-nav a` | **15 / 15** | 15 / 15 | **16 / 16** | 500 |
| Телефон футера | `.foot-phone` | **15 / 15** | 15 / 15 | **16 / 16** | 600 |
| Логотип | `.wm .l1/.l2` | **16 / 16** | — | — | 700 |
| Логотип в меню | `.mm-wm .l1/.l2` | **14 / 14** | — | — | 700 |
| Меню (пункт) | `.mm-nav a` | **20 / 24** | — | — | 500 |
| Телефон в меню | `.mm-phone` | **18 / 18** | — | — | 700 |
| Поле ввода | `.lfield` `.mfield` | **16 / 16** | — | — | 400 |
| Таб (заголовок) | `.rl-tab b` | **16 / 18** | — | — | 700 |
| Таб (подпись) | `.rl-tab span` | **12 / 12** | — | — | 500 |
| Счётчик слайдера | `.s-count` | **14 / 14** | — | — | 600 |
| Подсказка генплана | `.gp-hint` | **14 / 18** | — | — | 500 |
| Переключатель варианта | `.varbtn` `.zoomhint` | **12 / 12** | — | — | 500 |

### 4.5 Надзаголовки и подписи

| Роль | Класс | Размер | Вес | letter-spacing | Регистр |
|---|---|---|---|---|---|
| Eyebrow | `.eb` `.fcard-eb` | **12 / 12** | 600 | `.24em` | UPPERCASE |
| Eyebrow на фото | `.hcard-eb` `.mtag` | **12 / 12** | 600 | `.2em` | UPPERCASE |
| Метка в модалке | `.mh` `.msec .mlabel` | **12 / 12** | 600 | `.16em` | UPPERCASE |
| Eyebrow с локацией | `.eb-loc` (2 строки) | **12 / 18** | 600 | `.24em` | UPPERCASE |
| Сноска | `.mnote` | **12 / 18** | 400 | — | — |
| Ошибка формы | `.phone-error` `.form-error` | **12 / 16** | 500 | — | цвет `#C0392B` |
| Копирайт | `.foot-credit` | **13 / 13** | 500 | `.01em` | — |
| Мелкий текст | `.hero-fine` | **14 / 20** | 400 | — | — |

---

## 5. Кнопки

Базовый класс — **`.pill`** (`src/styles/layout.css`). Работает и на `<a>`, и на `<button>`.

```css
.pill {
  height: var(--button-h);            /* 48px */
  width:  var(--button-width, auto);  /* задаётся по месту использования */
  padding: 0 var(--button-pad-x);     /* 24px */
  gap: 8px;
  border-radius: var(--radius-pill);  /* 999px */
  font: 600 15px/1 'Onest', sans-serif;
  letter-spacing: .005em;
  white-space: nowrap;
  flex: none;
}
```

| Параметр | Desktop | Tablet | Mobile |
|---|---|---|---|
| Высота | **48px** | 48px | 48px |
| Высота CTA в модалке (`.lsubmit`, `.mcta`) | **56px** (`--field-h`) | 56px | 56px |
| Padding | `0 24px` | `0 24px` | `0 24px` |
| Кегль | 15px | 15px | **16px** |
| Радиус | 999px | 999px | 999px |

### 5.1 Варианты

| Класс | Фон | Текст | Hover |
|---|---|---|---|
| `.pill-dark` | `--c-green` | `--c-bg` | фон → `--c-green-dk` |
| `.pill-accent` | `--c-accent` | `--c-cream-3` | фон → `--c-accent-dk` |
| `.pill-ghost` | прозрачный, рамка `#C7C2B4` | `--c-ink` | фон и рамка → `--c-ink`, текст → `--c-bg` |
| `.pill-light` | `--c-bg` | `--c-ink` | фон → `#fff` |

### 5.2 Ширины

**Ширина кнопки не должна выводиться из ширины текста.** Задавайте `--button-width`
в правиле по месту использования — кратно 4 или 8, ближайшее к визуальной ширине.

| Кнопка | Селектор | Desktop / Tablet | Mobile |
|---|---|---|---|
| «Замовити дзвінок» | `.hdr-r .pill` | **192px** (только desktop — `.hdr-r` скрыт ≤1024) | скрыта |
| «Отримати актуальну вартість» | `.hero-cta .pill-accent` | **276px** | `100%` |
| «Переглянути будинки» | `.hero-cta .pill-ghost` | **216px** | `100%` |
| «Дивитися на Google картах» | `.loc-copy .pill` | **256px** | `100%` |
| «Отримати пропозицію» | `.fcard .pill-accent` | **224px** | `100%` |
| «Записатися на презентацію» | `.fcard .pill-light` | **264px** | `100%` |
| «Більше зображень» | `.rl-more .pill` | **192px** | **208px** |
| «Отримати розрахунок ремонту» | `.re-fab` | **288px** | **304px** |
| CTA в модалке дома | `.mcta` | **276px** | `100%` |
| Submit лид-формы | `.lsubmit` | `100%` | `100%` |
| Кнопка в мобильном меню | `.mm-foot .pill` | `100%` (меню есть только ≤1024) | `100%` |

**Как подобрать ширину новой кнопки:**
1. Временно поставьте `--button-width: auto`, замерьте фактическую ширину в DevTools.
2. Округлите до ближайшего кратного 8 (или 4).
3. Проверьте запас: `ширина ≥ ширина_текста + 48` (padding с двух сторон). Держите
   запас **не меньше 4px** — иначе при фолбэке шрифта текст обрежется (`overflow:hidden`).

### 5.3 Анимация подписи

Текст кнопки обёрнут в «ролл» — при наведении уезжает вверх, снизу приходит дубль:

```html
<a class="pill pill-accent" href="#">
  <span class="roll"><span class="roll-in" data-text="Текст">Текст</span></span>
</a>
```

`data-text` **обязателен** и должен совпадать с содержимым — из него рисуется вторая строка.
Окно ролла — `line-height: 20px` (в px, не множителем).

### 5.4 Иконочные кнопки

| Элемент | Desktop | Tablet | Mobile | Радиус |
|---|---|---|---|---|
| `.navbtn` (бургер), `.phone-ic` | — | **48** | 48 | 999 |
| `.mm-close` | — | **44** | 44 | 999 |
| `.s-btn` (стрелки слайдера) | **48** | 48 | **44** | 999 |
| `.mclose` (крестик модалки) | **44** | 44 | 44 | 999 |
| `.hcard-go` (стрелка на карточке) | **44** | 44 | 44 | 999 |
| `.plan-close`, `.rl-lb-close` | **48** | 48 | 48 | 999 |
| `.rl-lb-nav` (лайтбокс) | **48** | 48 | **44** | 999 |
| `.loc-mag` (лупа на карте) | **56** | **48** | 48 | 50% |
| `.adv-ic` (иконка преимущества) | **64** | **60** | 60 | 16px |
| `.gp-badge` (номер дома) | **26** | **22** | 18 (≤480) | 999 |

---

## 6. Поля ввода

```css
.lfield, .mfield {
  height: var(--field-h);      /* 56px */
  padding: 0 24px;
  border: 1px solid var(--c-line-field);
  background: var(--c-field);
  border-radius: var(--radius-md);   /* 14px */
  font: 400 16px/16px 'Onest', sans-serif;
  color: var(--c-ink);
}
.lfield::placeholder { color: var(--c-grey); }
.lfield:focus {
  border-color: var(--c-green);
  box-shadow: 0 0 0 3px rgba(41,66,57,.12);
}
```

| Параметр | Значение на всех ступенях |
|---|---|
| Высота | **56px** |
| Padding | `0 24px` |
| Радиус | `14px` |
| Кегль | `16px / 16px` (16px обязателен — иначе iOS зумит страницу при фокусе) |
| Зазор между полями | `12px` (`.lfields`) |
| Зазор поле↔кнопка | `12px` (`.mform-row`) |

Ошибка выводится отдельным блоком `.phone-error` / `.form-error` — `12/16`, `#C0392B`, `margin-top:8px`.

---

## 7. Отступы и сетки

### 7.1 Шкала spacing

Используйте **только эти значения** для `padding` / `margin` / `gap`:

```
4 · 8 · 12 · 16 · 20 · 24 · 32 · 40 · 48 · 56 · 64 · 80 · 96 · 120
```

### 7.2 Сетки — gap подобран так, чтобы колонки делились нацело

| Сетка | Desktop (контент 1296) | Tablet (688) | Mobile (350) |
|---|---|---|---|
| `.hero` | `596 + 56 + 644` (явно, ≥1440) | `display:block` | `display:block` |
| `.trail`, `.comms` | 2 × **632**, gap **32** | 1 колонка, gap 24 | 1 колонка |
| `.tpair` | 2 × **312**, gap **8** | 2 × **340**, gap 8 | 1 колонка |
| `.stack` (преимущества) | 4 × **312**, gap **16**; `.big` = **640** | 2 × **336**, gap 16 | 1 колонка |
| `.rl-set.on` (галерея) | 4 × **312**, gap **16** | 3 × **224**, gap **8** | 2 × **170**, gap **10** |
| `.rl-head` | 2 × **620**, gap **56** | 1 колонка, gap 16 | 1 колонка |
| `.foot-cta` | 2 × **636**, gap **24** | 1 колонка, gap 16 | 1 колонка |
| `.loc` | `536 + 56 + 704` (явно, ≥1440) | 1 колонка, gap 24 | 1 колонка |
| `.track` (слайдер) | карточка **384**, gap **20** | 384, gap 16 | **280**, gap 16 |

> Явные px-колонки (`.hero`, `.loc`) заданы внутри `@media (min-width:1440px)` — на
> промежуточных ширинах работает `fr`, чтобы не ломать адаптивность.

### 7.3 Карточки

| Карточка | Desktop | Tablet | Mobile | Padding | Радиус |
|---|---|---|---|---|---|
| `.hcard` (дом) | **384 × 512** | 384 × 512 | **280 × 384** | — | 22 |
| `.adv-card` | **312 × 280** | **336** | **350** | **24** | 20 |
| `.adv-card.big` | **640 × 280** | **688** | 350 | 24 | 20 |
| `.tcell` | **312 × 286** | **340 × 210** | **350 × 190** | **32 / 24 / 24** | 18 |
| `.rl-cell` | **312 × 312** | **224 × 224** | **170 × 170** | — | 14 |
| `.fcard` | **636 × 416** | **688 × 300** | **350** | **40 / 32 / 24** | 24 |

### 7.4 Медиа-блоки

| Блок | Desktop | Tablet | Mobile |
|---|---|---|---|
| `.hero-media` | **768 × 480** | **688 × 388** | **350 × 264** |
| `.loc-map` | **704 × 396** | **688 × 387** | 350 × 196.9 * |
| `.gp-stage` | 1296 × 971.4 * | 688 × 515.7 * | 350 × 506.9 * |
| `.lmedia` (лид-модалка) | **488 × 500** | **352 × 432** | **366 × 228** |
| `.planwrap` | **376** | **688 × 516** | **334 × 250** |
| `.planwrap.zoomed` | во весь вьюпорт | во весь вьюпорт | во весь вьюпорт |

\* **`.gp-stage` и `.loc-map` — исключение: высота задаётся `aspect-ratio`, не фиксированным
значением.** Поверх них лежат хотспоты и пины, спозиционированные в `%` от контейнера, поэтому
пропорции обязаны совпадать с картинкой на **любой** ширине, а не только на контрольной.
Фиксированная высота + `object-fit:cover` ломает привязку везде между брейкпоинтами
(на 360px промах доходил до 9%). Дробная высота здесь допустима — это масштабирование
изображения (см. [Допустимые дроби](#допустимые-дроби)).

| Блок | `aspect-ratio` | Источник |
|---|---|---|
| `.gp-desk` | `1030/772` | `genplan-new-desktop.jpg` 1030 × 772 |
| `.gp-mob` | `533/772` | `genplan-new-mobile.jpg` 533 × 772 |
| `.loc-map` | `16/9` | кадрирует фото через `object-fit:cover` (так задумано) |

> Колонки `.loc` на десктопе — `536 + 56 + **704**`: 704 кратно 16, поэтому `16/9`
> даёт целое **396**. Если будете менять ширину карты — сохраните кратность 16.

> 🔁 **При замене картинки генплана** обязательно обновить три места:
> 1. `src="assets/…"` в `index.html` (два тега — `.gp-desk` и `.gp-mob`);
> 2. `aspect-ratio` в `genplan.css` — точные пиксельные размеры нового файла;
> 3. `GP_POS` / `GP_POS_MOB` в `src/js/data.js` — координаты хотспотов в `%`.
>
> Порядок элементов в `GP_POS` привязан к `GP_NO = [4,3,2,1,8,7,6,5,9,10,11]`:
> сначала левая колонка сверху вниз, потом правая, потом верхний ряд слева направо.
> Менять порядок нельзя — по нему через `SECTION_MAP` определяется планировка.

### 7.5 Строки-ряды — фиксированная высота вместо padding

У этих элементов есть `1px` разделитель. Высота задана явно, `box-sizing: border-box`
включает бордер, `padding: 0`, вертикальное центрирование — через `align-items: center`.
Обязателен **`flex: none`**, иначе элемент сожмётся внутри flex-колонки.

| Элемент | Desktop | Tablet | Mobile |
|---|---|---|---|
| `.hero-blist li` | **48** | 48 | 48 |
| `.crow` | **128** | **88** | 88 |
| `.feats li` | **42** | 42 | 42 |
| `.msec` | **76** | 76 | 76 |
| `.mm-nav a` | — | **56** | 56 |

---

## 8. Модальные окна

| Параметр | Desktop | Tablet | Mobile |
|---|---|---|---|
| `.mscrim` padding | **40** | **24** | **12** |
| `.modal` max-height | `calc(100vh - 80px)` | `calc(100vh - 48px)` | `calc(100vh - 24px)` |
| Что скроллится | `.mright` (колонка) | `.modal` целиком | `.modal` целиком |
| `.planwrap.zoomed` padding | **64** | **40** | **16** |
| Модалка дома `.modal` | **1080** | **720** | **366** |
| Лид-модалка `.lmodal` | **1000** | **720** | **366** |
| `.mbody` колонки | `440 + 640` | `display:block` | `display:block` |
| `.lbody` колонки | `488 + 512` | `352 + 368` | колонка (≤720) |
| `.mleft` padding | **32** | **16** | 16 |
| `.mright` padding | **40** | **24** | `24 20` |
| `.lform` padding | **48** | **32** | **24** |
| `.mform` padding | **24** | **20** | 20 |
| Радиус модалки | 22 | 22 | 22 |

`max-height` через `calc(100vh − отступы)` — даёт целое число при любой чётной высоте окна
(`92vh` давало дробь).

> ⚠️ На `≤1024px` `.mbody` становится `display:block`, поэтому внутренняя колонка `.mright`
> больше не скроллит — скроллить должен сам `.modal`. За это отвечает
> `@media (max-width:1024px){ .modal{overflow-y:auto} }` в `responsive.css`.
> Без него базовый `.modal{overflow:hidden}` обрезает контент и до формы не добраться.

---

## 9. Радиусы, тени, слои, анимации

### Радиусы

| Токен | Значение | Где |
|---|---|---|
| `--radius-pill` | `999px` | кнопки, чипы, иконочные кнопки |
| `--radius-xl` | `24px` | `.fcard`, `.loc-map` |
| `--radius-lg` | `22px` | `.modal`, `.hcard`, `.hero-media`, `.gp-stage` |
| `--radius-md` | `14px` | поля ввода, `.rl-cell`, `.rl-tab`, `.planwrap` |
| — | `20px` | `.adv-card` |
| — | `18px` | `.tcell` |
| — | `16px` | `.adv-ic`, `.mform` |
| — | `10px` | `.room` |

### Тени

```css
/* модалка */      box-shadow: 0 44px 100px rgba(20,26,22,.32);
/* мобильное меню */ box-shadow: 1px 0 0 var(--c-line), 2px 0 46px rgba(20,26,22,.22);
/* плавающая CTA */  box-shadow: 0 14px 34px rgba(23,32,29,.24);
/* фокус поля */     box-shadow: 0 0 0 3px rgba(41,66,57,.12);
```

> Разделители `.foot-bar`, `.mnote` и правая граница `.mm` нарисованы через
> `box-shadow: inset 0 1px 0 …`, а не `border` — бордер прибавлялся к auto-высоте
> и делал её нечётной.

### Z-index

| Слой | Значение |
|---|---|
| `.hdr` (шапка) | `40` |
| `.mm-scrim` / `.mm` (меню) | `55` / `60` |
| `.mscrim` (модалки) | `80` |
| `.re-fab` (плавающая CTA) | `90` |
| `.planwrap.zoomed` | `95` |
| `.msgr` (виджет связи) | `100` |
| `.rl-lb` (лайтбокс) | `120` |
| `.preload` | `200` |

### Анимации

| Что | Значение |
|---|---|
| Смена цвета / фона / рамки | `.2s` |
| Появление модалки | `.34s cubic-bezier(.2,.7,.2,1)` |
| Открытие меню | `.36s cubic-bezier(.4,0,.2,1)` |
| Ролл текста кнопки | `.45s cubic-bezier(.55,0,.2,1)` |
| Зум фото в карточке | `.6s cubic-bezier(.2,.6,.2,1)` |
| Reveal при скролле | `.55s` |

Все декоративные анимации обёрнуты в `@media (prefers-reduced-motion: reduce)` — при
добавлении новых делайте так же.

---

## 10. Правила вёрстки

Макет готовится под импорт в Figma: **внешние рамки компонентов на 1440 / 768 / 390 px
должны быть целыми числами, преимущественно чётными.**

### Нельзя

- ❌ `clamp()` и `vw` для типографики, отступов, размеров компонентов, кнопок, карточек.
  Единственное исключение — `100vw` в `margin-right` у `.hero-media` (вынос под край экрана).
- ❌ `line-height` множителем (`1.5`, `1.04`) — только px.
- ❌ Ширина кнопки «по тексту». Ставьте `--button-width`.
- ❌ Общий `min-width` на все кнопки.
- ❌ `max-width` в `ch` — даёт дробную ширину текстового блока. Только px.
- ❌ `aspect-ratio` там, где ширина не делится нацело — **кроме блоков с `%`-оверлеями**
  (`.gp-stage`, `.loc-map`): там наоборот обязателен `aspect-ratio`, иначе привязка
  хотспотов/пинов поедет между брейкпоинтами. В остальных случаях задавайте `height` по ступеням.
- ❌ Значения `padding` / `margin` / `gap` вне [шкалы spacing](#71-шкала-spacing).

### Нужно

- ✅ Фиксированные ступенчатые значения через `@media`.
- ✅ Кегли и `line-height` — целые px, преимущественно чётные.
- ✅ У элементов с фиксированными размерами внутри flex — **`flex: none`**.
- ✅ Строкам с `1px` разделителем — явная `height` (бордер входит в неё,
  т.к. глобально задан `box-sizing: border-box`).
- ✅ `<button>` без своего `font-size` наследует UA-дефолт `13.3333px` — в `base.css`
  для всех кнопок принудительно задан `font-size: 16px`.

### Допустимые дроби

Реальная ширина глифов текста и внутренние текстовые измерения браузера, `transform`,
масштабирование изображений. Сейчас дробную ширину имеют только текстовые узлы:
`.h-title`, `.gp-title`, `.crow-t`, `.wm`, `.rl-tab b`, `.foot-credit`, `.eb-loc` —
их **рамки** (`.h-head`, `.crow`, `.hdr-in`, `.rl-tab`, `.foot-bar`) целые и чётные.

### Как проверить свою правку

DevTools → выставить ширину окна 1440 / 768 / 390 → у нового блока в «Computed»
проверить `width`, `height`, `font-size`, `line-height`, `padding`, `gap`, `border-radius`.
Дробей быть не должно.

---

## 11. Структура файлов

```
src/styles/
  main.css            — только @import, задаёт порядок каскада
  tokens.css          — цвета, радиусы, --container-pad, --section-pt, --button-h, --field-h
  base.css            — reset, body, прелоадер
  layout.css          — .wrap, .eb, .pill (базовая кнопка)
  header.css          — шапка + мобильное меню
  hero.css  terms.css  houses.css  tech.css  communications.css
  advantages.css  remont.css  genplan.css  location.css  footer.css
  modal.css           — обе модалки, поля форм
  contact-widget.css  — плавающий виджет связи (сейчас отключён в main.js)
  stats.css           — неиспользуемая секция
  responsive.css      — подключается ПОСЛЕДНИМ, здесь оверрайды ≤1024 и ≤560
```

Мёртвые правила (классов нет ни в HTML, ни в JS): `.stats`, `.stat-*`, `.terms-cta`,
`.hero-sub`, `.hero-ticker*`, `.adv-head/-title/-sub/-eb`, `.terms-eb`, `.tech-eb`,
`.comms-sub`, `.fcard-feats`, `.rl-foot`, `.rl-note`, `.pill-badge`, `.pill-ic`.
Они приведены к тем же правилам, но при подключении сначала проверьте актуальность.
